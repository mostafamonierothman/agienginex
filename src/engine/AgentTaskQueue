export class AgentTaskQueue {
  static async enqueue(task: any) {
    console.log('Task queued:', task);
    // Optionally log to Supabase or a local file
    return true;
  }

  static async dequeue() {
    // Return a dummy task for now
    return null;
  }
}
