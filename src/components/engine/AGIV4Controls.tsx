
// AGIV4Controls intentionally removed (V4 deprecated, V5+ in use). 
// This stub prevents build errors if imported elsewhere.

import React from "react";
const AGIV4Controls = () => (
  <div className="p-4 text-center text-gray-400">
    AGI V4 Controls are no longer available in this version.
  </div>
);
export default AGIV4Controls;
